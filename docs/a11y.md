# Accessibility

Before this, accessibility here was one afternoon of fixes in April 2026 and a
hope that nothing regressed. This file describes what now holds that line
automatically, what it deliberately does not hold, and what only a human can
check.

## The automated suite

`tests/e2e/a11y.spec.ts` runs [axe-core](https://github.com/dequelabs/axe-core)
through `@axe-core/playwright` against the real application, and adds keyboard
journeys that axe cannot see.

```bash
pnpm exec playwright test tests/e2e/a11y.spec.ts
```

It is part of `pnpm test:e2e`, so the PR-preview workflow already runs it on
every pull request from this repository, against the deployed preview image.
There is no separate a11y job: an accessibility failure is a failing end-to-end
suite, which is the point.

### The bar

Only `serious` and `critical` violations fail the suite. `moderate` and `minor`
findings are still reported by axe but do not gate: they are mostly
best-practice hints, and gating on them would make the suite noisy enough that
people stop reading it.

Rules are scoped to the WCAG tags `wcag2a`, `wcag2aa`, `wcag21a`, `wcag21aa`.

### What it scans

Both colour schemes, and a 390x844 phone viewport for the surfaces that change
shape there:

| Surface                                        | Why it earns a scan                                    |
| ---------------------------------------------- | ------------------------------------------------------ |
| Home page                                      | The only page that is not the list                     |
| Listen later page, empty and populated         | Empty state and table are different trees              |
| Search results, skeletons then options         | The skeleton is a distinct DOM, live for seconds       |
| Offline alert                                  | Renders only when the browser goes offline             |
| Add dialog: loading, loaded, duplicate, failed | Four states, one component                             |
| Delete confirmation dialog                     | The only destructive path                              |
| Toasts, success and error                      | Added by `c19e379`; live regions are easy to get wrong |
| Listen later cards (mobile)                    | The table becomes cards below `md`                     |
| Navigation sheet (mobile)                      | A dialog that only exists on small screens             |
| Add dialog at a phone width                    | Renders as a sheet, not the desktop dialog             |

Dialog scans are scoped to `[role="dialog"]` and additionally assert
`aria-modal="true"`. bits-ui leaves the page behind a modal in the accessibility
tree and relies on the focus scope plus `aria-modal` to keep a screen reader
inside, so scanning the whole page would measure the dimmed background against
its own overlay and report contrast failures nobody can read.

Animations are settled before every scan. Contrast is measured against whatever
is painted at that instant, and a scan fired mid-transition blends colours with
the overlay they are fading over.

### What it does not scan

The error pages. `inertia/pages/errors/not_found.svelte` and
`server_error.svelte` are AdonisJS starter stubs that nothing renders: the
exception handler declares no `statusPages`, so a missing route answers with raw
JSON and neither page is reachable. `errors_controllers.ts` is dead beside them,
unrouted and rendering an `errors/unauthorized_access` page that does not exist.
Issue #36 listed these surfaces on the assumption they existed; scanning a page
that never renders measures nothing. The three artefacts and the product
decisions they need are written up in #54.

### Keyboard journeys

- the paste-link journey completes with no mouse at all
- the add dialog traps focus, closes on `Escape`, and gives focus back
- the delete dialog closes on `Escape` and returns focus to the row menu
- arrow keys reach the search results and scroll them into view
- the row menu hands focus to its first item only once the menu is on screen

## The allowlist

One rule is parked, in `ALLOWED_RULES`:

**`scrollable-region-focusable`** — the search results popup is a
`max-h-[300px] overflow-y-auto` region. axe sees a scrollable container with no
tabbable descendant and reports that a keyboard user cannot scroll it. That is
true of `Tab` and false of the widget: the results are a `role="listbox"` owned
by a `role="combobox"` input, and the ARIA authoring practice is precisely that
options are _not_ in the tab order — the combobox keeps focus and arrow keys
move a roving `tabindex` through the options, which scrolls them into view. The
test `arrow keys reach and scroll the search results` measures that: it presses
`ArrowDown` past the fold and asserts the region actually scrolled and the
focused option is inside the visible box.

Empty is the goal state for this list. Every entry owes an explanation here.

## Colour

Every token pair below is measured from `resources/css/app.css` as it stands.
Text needs 4.5:1 (WCAG 1.4.3); control boundaries and icons need 3:1 (1.4.11).

| Pair                                      | Needs | Light | Dark  |
| ----------------------------------------- | ----- | ----- | ----- |
| `foreground` on `background`              | 4.5   | 19.90 | 19.05 |
| `muted-foreground` on `background`        | 4.5   | 5.21  | 7.77  |
| `muted-foreground` on `muted`             | 4.5   | 4.74  | 5.81  |
| `input` border on `background`            | 3     | 3.25  | 3.56  |
| `ring` on `background`                    | 3     | 5.11  | 4.42  |
| `destructive` as text on `background`     | 4.5   | 6.46  | 6.04  |
| `destructive-foreground` on `destructive` | 4.5   | 6.18  | 5.44  |
| `warning` icon/border on `background`     | 3     | 3.29  | 7.46  |
| `warning-foreground` on `background`      | 4.5   | 12.32 | 18.45 |

Four of those were failing and moved:

- **`muted-foreground`** 46.1% -> 44%. It read 4.39:1 on `muted`, just under AA,
  and `muted` is where secondary text most often sits.
- **`input`** was the same value as `--border`, 90% light and 15.9% dark. A
  decorative edge owes nothing, but the boundary that tells you where a control
  is owes 3:1. They are now different tokens with different jobs.
- **`ring`** 64.9% -> 45% light. A focus ring at 2.56:1 is not a focus ring.
- **`destructive`** is read as text five times over and used as a fill once, so
  it became the readable ink and `destructive-foreground` answers to the fill.
  In dark mode no single red is legible on near-black _and_ takes white on top,
  so dark picks a light red for the ink and darkens the fill's label instead.

Two hardcoded Tailwind greens also moved, because they carry state:

- listened badge `text-green-600` -> `text-green-800` in light: 3.00 -> 6.49
- toast success icon `text-green-500` -> `text-green-800` in light: 2.28 -> 7.13

The dark variants (`green-400`, 8.54 and 11.42) already passed and are unchanged.
The amber alert variant now uses the `--warning` token instead of hardcoded
`amber-500`/`amber-600`, which is what that token was introduced for.

Purely decorative borders (`--border`, `--sidebar-border`) stay below 3:1
deliberately. 1.4.11 applies to boundaries that identify a control, not to
every line on the page.

## Motion

`prefers-reduced-motion: reduce` is honoured everywhere, and it means _remove
the travel, keep the fade_. Opacity carries the state change; nothing slides,
scales, or spins. A surface that vanished instead of fading would be a
regression too — the change has to stay legible.

Most of it is one block in `resources/css/app.css`. tailwindcss-animate routes
every `animate-in`/`animate-out` through `--tw-enter-*`/`--tw-exit-*` custom
properties, so neutralising those covers the dialog, sheet, dropdown, select,
tooltip and popover at once; the dialog additionally swaps its scale curve for
the overlay's, and `.animate-spin` becomes a pulse. Three surfaces cannot be
reached from CSS and guard themselves: `data_table_row_transitions.ts`,
`data-table-status-badge.svelte`, and the highlight scroll in
`listen-later.svelte`. Toast motion belongs to `svelte-sonner`, which ships its
own `prefers-reduced-motion` block.

The gate is `no surface travels once the OS asks for reduced motion`. It hooks
`animationstart`, `transitionstart` and `Element.prototype.animate` before the
page loads, walks the app under emulated `reduce`, and asserts no keyframe
carries a transform that resolves to anything but the identity matrix. Every
guard above was mutated and the gate caught each one.

Two blind spots shape how that test is written:

- **A full turn is an identity matrix at both ends.** `rotate(360deg)` resolves
  to `matrix(1, 0, 0, 1, 0, 0)`, so the spinner reads as motionless through its
  keyframes. Hence the direct `animation-name` assertion next to the loader.
- **Svelte skips a local `out:` when its parent block is destroyed.** Deleting
  the only row empties the list and takes the table with it, so the row
  transition never runs. The test seeds two rows and deletes one.

## The manual pass

**Status: run on 2026-08-07**, Safari 18 + VoiceOver, macOS 15. Eleven defects,
all but one invisible to axe because every attribute involved was already
correct - the problem was what a reader is given, not what the markup says.

Safari because VoiceOver is a WebKit-first screen reader: it is the pairing
Apple builds and tests against, and the one VoiceOver users are actually on.
Chrome and Firefox on macOS reach VoiceOver through cross-process bridges with
their own bugs, so a finding there may be the browser rather than this app.

Two settings first, or Tab will appear to skip every link on the page - macOS
limits Tab to text boxes and lists out of the box:

- System Settings > Keyboard > **Keyboard navigation**
- Safari > Settings > Advanced > **Press Tab to highlight each item on a webpage**

Restart Safari after changing them. Note also that Tab is not how macOS expects
you to reach things: `VO+Cmd+L` steps link to link, `VO+Cmd+H` heading to
heading, and `VO+U` opens the rotor.

### What the pass found

**A dialog was never named.** bits-ui focuses the first tabbable child on open,
so pasting a link opened the add dialog straight onto "Title, edit text". The
dialog's own name and description are only spoken when focus lands on the
container that carries them. Same defect in the delete confirmation, where the
question under the heading went unread. Focus now moves to the content element,
which already holds both.

**The duplicate state changed silently.** The dialog opens on "Processing Link"
while the fetch is in flight and rewrites its title when the answer lands. A
dialog name is spoken once, on focus, so the reader was left holding the loading
state. An `sr-only` `role="status"` announces the resolution instead.

**Two select triggers read as bare values.** A `<label for>` cannot label a
button; where Safari honoured it at all it replaced the name rather than
prefixing it, so the chosen type went unread. Both triggers now point
`aria-labelledby` at the label _and_ at themselves.

**A result option withheld its artists.** "Add Nightcall to listen later" is
ambiguous the moment a search returns Another Version - the whole distinction
between two recordings lives in the artist set, which is exactly what the name
dropped. The name now carries both.

**The listbox did not own its options.** VoiceOver read "you are currently on a
selectable (null), inside a list box": `role="listbox"` may only own `option`
children, and the loading skeleton left two generic wrappers between them. The
skeleton now wraps each `<li>` from the inside.

**The table was read a second time, and it did have defects.** The first write-up
cleared it on the strength of one journey; walking the rows with `VO+arrow` and
the rotor found five more, all confirmed against the accessibility tree before
anything was changed:

- **Every row menu was called "Open menu".** The rotor lists them together, out
  of the table's reading order, where identical names name nothing. Each trigger
  now says which item it opens.
- **No cell headed its row.** Reading across, VoiceOver prefixes each cell with
  the row header; there was none, so the cells arrived unattached. The title cell
  is now a `<th scope="row">`, and the title column can no longer be hidden.
- **Sorting was silent.** The order changes under the reader with no focus
  change. An `sr-only` `role="status"` now speaks the new order, and it starts
  empty - a live region that arrives already full announces nothing.
- **Marking an item listened said nothing.** Every other list write raises a
  toast; this one only repainted a badge on a row the reader had already passed.
- **The delete dialog described nothing.** "Are you sure?" and "Confirm" name no
  item. Both now carry the title and the artists of what is about to go.

**The search fields never said which key reaches the results.** The options are
`tabindex="-1"` until focused, so Tab walks past them; only ArrowDown gets in,
and nothing on screen or in the accessibility tree said so. Both fields now point
at one `sr-only` hint - and the artist field, which had no key handler at all,
now answers the same keys as the title field.

### A third pass

**Status: run on 2026-08-07**, same rig. Three defects, all in places an earlier
pass had already touched and declared done - each fix had named the container
and left what it holds unnamed.

**A result option was named by a summary of itself.** The option printed four
lines - title, artists, album, release date - and carried an `aria-label`
reading "Add Nightcall by Kavinsky to listen later". A label replaces the
contents rather than introducing them, so the album and the release date, both
on screen, were never spoken; VoiceOver announced a selectable with no relation
to what it showed. The label is gone and the option is named by its own text.
What Enter will do moved to `aria-describedby`, which appends instead.

**Header cells carried no `scope`.** The previous pass gave each row a `<th
scope="row">` and stopped there. Reading across, VoiceOver pairs a value with
its column only when the pairing is stated: with the header cells unscoped it
read "Daft Punk" and never said that was the Artists column. Every `<th>` in the
head is now `scope="col"`.

**The row menu named its item and then dropped it.** The trigger says which
recording it opens; the two items inside said "Mark as listened" and "Delete".
The menu floats away from its row, so by the time it is open the item is the one
thing missing. Both now carry the title and the artists in an `sr-only` tail,
after the visible verb so the button still reads as one word on screen.

Reading the table for those two also caught its badge icons. Each one sits next
to the word it stands for, and none was hidden, so a reader crossing a row met
an "image" before every value. The cover is the only image in the table that
says anything; the rest are `aria-hidden` now.

Whether Inertia announces the page title on navigation is still unanswered - it
swaps the DOM without a document load, so the announcement is not free.

Every fix above is now gated by the `keyboard`, `announcements` and `accessible
names` blocks in `tests/e2e/a11y.spec.ts`. Each assertion was mutation-proven:
the fix was reverted and the test watched to fail.

### A fourth pass

**Status: run on 2026-08-08**, same rig. Two defects, both about what a control
carries with it rather than what it is called.

**A row's tab stops said nothing about their row.** Tab enters a row, lands on
its links and then its menu button; the cover, status, type, artists and date
sit in cells it jumps straight over. "Spotify" never said which recording it
streams, so tabbing the table was a run of platform names with nothing telling
one row from the next. Every control in a row is now `aria-describedby` a
summary of it - title, type, artists, listened state, added date - so the row
arrives with the control that belongs to it. That summary is `aria-hidden` and
reached by id: a description target is still read when it is hidden, and left in
the reading order it would say the row twice to anyone walking the cells.

**The first item of a menu opened silent.** bits-ui focuses the menu in the
frame it is inserted and its first item a fraction of a millisecond after that,
both before the open animation has painted anything. VoiceOver reads the menu
appearing and drops the focus move inside it, so "Mark as listened" was never
spoken while every item reached by an arrow key afterwards read correctly.
`dropdown-menu-content.svelte` now takes `onOpenAutoFocus` over: it waits for
the content's animations to finish and one frame beyond that, then focuses - a
single announcement, on something the reader can see. A focus arriving after the
menu closed is dropped, so `Escape` inside that gap still leaves the trigger
focused.

Both are gated in `tests/e2e/a11y.spec.ts` and were mutation-proven the same
way: the row summary by the `aria-describedby` it did not have, the focus timing
by reverting the handler and watching the menu's opacity read `0` at focus.

### A fifth pass

**Status: run on 2026-08-08**, same rig. Two defects, both in a component rather
than in a page.

**A select could not say what it held.** The Status and Type filters read as a
control and then went quiet: moving through the options announced nothing, and
the chosen value was never spoken back. shadcn-svelte's `Select` is a
`<button aria-haspopup="listbox">` driving a portalled `role="listbox"`, and a
button takes no `aria-activedescendant` - bits-ui sets one, the platform ignores
it there, so the option under the arrow keys existed only visually. Nothing
about it was fixable from the call site.

All five selects - both table filters, the mobile sort, the search type and the
dialog's item type - are now a native `<select>`
(`inertia/lib/components/ui/select/select.svelte`) wearing the same skin, and
the bits-ui wrappers are deleted. The platform carries the value, the label and
the options; on a phone it opens the system picker, which is also why the
touch-target test no longer measures the options - they are drawn outside the
page.

A native select is named by a real `<label for>`, so "Status:" and "Type:" are
now visible beside their controls instead of being folded into the value. The
accessibility tree gained the pairing for free: `toHaveValue` replaces the
`aria-label` assertions, since a value the platform owns cannot drift from what
is shown.

**A ticked column said nothing.** `Columns` is a menu of
`role="menuitemcheckbox"`, which is the right shape - but bits-ui closes a menu
on select by default, checkbox items included. Ticking one destroyed the thing
that had just changed: the menu unmounted, focus landed back on the trigger, and
the reader announced "Columns, menu pop-up button" rather than the new state.
Nothing was ever spoken about the column. `closeOnSelect={false}` holds the menu
open, so `aria-checked` flips under the reader's cursor and is read as it flips

- and toggling three columns no longer means opening the menu three times.

`Columns` stays a menu rather than becoming a multi-select. A
`<select multiple>` is a scrolling box the user must ctrl-click into, with no
phone picker behind it and no way to see which rows a choice affects; the menu
is the pattern this control already is.
